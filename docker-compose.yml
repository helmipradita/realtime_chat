version: "3.9"

services:
  nextjs:
    container_name: realtime_chat_nextjs
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    restart: always
    ports:
      - "127.0.0.1:3000:3000"
